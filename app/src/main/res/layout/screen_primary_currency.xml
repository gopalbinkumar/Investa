<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/investa_background"
    android:orientation="vertical">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:paddingStart="12dp"
        android:paddingTop="18dp"
        android:paddingEnd="12dp">

        <ImageView
            android:id="@+id/primary_currency_back"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="start|center_vertical"
            android:contentDescription="@string/back"
            android:foreground="@drawable/ripple_icon"
            android:src="@drawable/ic_lucide_arrow_left"
            android:tint="@color/investa_icon_primary" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="@string/primary_currency"
            android:textColor="@color/investa_text"
            android:textSize="16sp"
            android:textStyle="bold" />
    </FrameLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingBottom="24dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="18dp"
                android:text="@string/choose_primary_currency"
                android:textColor="@color/investa_text_secondary"
                android:textSize="12sp" />

            <RadioGroup
                android:id="@+id/primary_currency_radio_group"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:background="@drawable/bg_surface"
                android:orientation="vertical"
                android:paddingStart="8dp"
                android:paddingTop="6dp"
                android:paddingEnd="8dp"
                android:paddingBottom="6dp"
                android:tag="investa_elevated_card">

                <RadioButton
                    android:id="@+id/primary_currency_idr"
                    android:layout_width="match_parent"
                    android:layout_height="52dp"
                    android:buttonTint="@color/theme_radio_tint"
                    android:gravity="center_vertical"
                    android:text="@string/primary_currency_idr"
                    android:textColor="@color/investa_text"
                    android:textSize="14sp" />

                <RadioButton
                    android:id="@+id/primary_currency_usd"
                    android:layout_width="match_parent"
                    android:layout_height="52dp"
                    android:buttonTint="@color/theme_radio_tint"
                    android:gravity="center_vertical"
                    android:text="@string/primary_currency_usd"
                    android:textColor="@color/investa_text"
                    android:textSize="14sp" />
            </RadioGroup>

            <TextView
                android:id="@+id/primary_currency_save"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginTop="24dp"
                android:background="@drawable/bg_primary_button"
                android:foreground="@drawable/ripple_button"
                android:gravity="center"
                android:text="@string/change_primary_currency"
                android:textColor="@color/investa_background"
                android:textSize="14sp"
                android:textStyle="bold" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>
